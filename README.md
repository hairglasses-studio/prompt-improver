# prompt-improver (archived)

This project has been merged into [ralphglasses](https://github.com/hairglasses-studio/ralphglasses).

- The enhancer library is at `internal/enhancer/`
- The standalone CLI is at `cmd/prompt-improver/`

Install from the ralphglasses repo:

```bash
cd ralphglasses
make install-prompt-improver
```
