name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    uses: hairglasses-studio/.github/.github/workflows/go-test.yml@main
    with:
      coverage-threshold: 0
